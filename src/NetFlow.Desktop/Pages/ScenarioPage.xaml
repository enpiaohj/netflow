<UserControl x:Class="NetFlow.Desktop.Pages.ScenarioPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="目标主机" Style="{StaticResource FieldLabelStyle}"/>
                        <ComboBox x:Name="TargetInput" IsEditable="True"
                                  Tag="IP 或域名" KeyDown="TargetInput_KeyDown"/>
                    </StackPanel>
                    <Grid Grid.Column="1" Margin="0,0,14,0">
                        <StackPanel x:Name="DomainPanel">
                            <TextBlock Text="域（可选）" Style="{StaticResource FieldLabelStyle}"
                                       ToolTip="用于 SRV / SPN 类查询中的 {domain} 占位符"/>
                            <ComboBox x:Name="DomainInput" IsEditable="True" Tag="如 corp.example.com"
                                      ToolTip="用于 SRV / SPN 类查询中的 {domain} 占位符"/>
                        </StackPanel>
                        <!-- SQL Server 场景：实例/端口（默认实例直测 TCP 1433；填实例名则经 Browser 1434 发现端口） -->
                        <StackPanel x:Name="SqlPanel" Visibility="Collapsed">
                            <TextBlock Text="SQL 实例 / 端口" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox x:Name="SqlInput" IsEditable="True"
                                      ToolTip="数字为固定端口（默认 1433）；实例名为命名实例（经 Browser UDP 1434 发现端口）">
                                <ComboBoxItem IsSelected="True">1433 默认实例（TCP 直连）</ComboBoxItem>
                                <ComboBoxItem>MSSQLSERVER 默认实例</ComboBoxItem>
                                <ComboBoxItem>SQLEXPRESS 命名实例（经 Browser 发现）</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                    <CheckBox Grid.Column="2" x:Name="CaptureToggle" Content="同时抓包"
                              VerticalAlignment="Bottom" Margin="0,0,14,8"
                              ToolTip="诊断期间同时抓包，需要管理员权限"/>
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                        <Button x:Name="RunButton" Content="开始诊断"
                                Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Click="Run_Click"/>
                        <Button x:Name="StopButton" Content="停止"
                                Style="{StaticResource SecondaryButtonStyle}" IsEnabled="False"
                                Click="Stop_Click"/>
                    </StackPanel>
                </Grid>
                <TextBlock x:Name="ProgressText" Text="" Margin="0,8,0,0"
                           Style="{StaticResource StatusLineStyle}"
                           Foreground="{StaticResource PrimaryBrush}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="2*" MinWidth="380"/>
                <ColumnDefinition Width="*" MinWidth="240"/>
            </Grid.ColumnDefinitions>

            <!-- 模板列表 -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                <DockPanel>
                    <Button DockPanel.Dock="Bottom" x:Name="SaveTemplateButton"
                            Content="保存为新模板" Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,8,0,0" Click="SaveTemplate_Click"/>
                    <TextBlock DockPanel.Dock="Bottom" x:Name="TemplateInfo" Margin="0,8,0,0"
                               Style="{StaticResource HintTextStyle}"/>
                    <TextBlock DockPanel.Dock="Top" Text="场景模板" Style="{StaticResource SectionTitleStyle}"/>
                    <ListBox x:Name="TemplateList" BorderThickness="0" Background="Transparent"
                             Margin="0,6,0,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" TextWrapping="Wrap" Padding="2,3"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- 检查结果 -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" x:Name="ResultTitle" Text="诊断概览"
                               Style="{StaticResource SectionTitleStyle}"/>
                    <!-- KPI 统计卡（对齐概念图 01 诊断概览） -->
                    <UniformGrid DockPanel.Dock="Top" x:Name="StatCards" Visibility="Collapsed"
                                 Columns="4" Margin="0,8,0,10">
                        <Border Style="{StaticResource KpiCardStyle}" Background="#F0FDF4" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets, Segoe Fluent Icons" FontSize="20" Foreground="#16A34A" VerticalAlignment="Center"/>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock x:Name="KpiPass" Text="0" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text="通过" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource KpiCardStyle}" Background="#FFFBEB" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets, Segoe Fluent Icons" FontSize="20" Foreground="#D97706" VerticalAlignment="Center"/>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock x:Name="KpiWarn" Text="0" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text="警告" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource KpiCardStyle}" Background="#EFF6FF" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE897;" FontFamily="Segoe MDL2 Assets, Segoe Fluent Icons" FontSize="20" Foreground="#475569" VerticalAlignment="Center"/>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock x:Name="KpiUnconfirmed" Text="0" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text="未确认" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource KpiCardStyle}" Background="#FEF2F2">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets, Segoe Fluent Icons" FontSize="20" Foreground="#DC2626" VerticalAlignment="Center"/>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock x:Name="KpiFail" Text="0" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text="失败" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                    <!-- 事件时间线（对齐概念图 01 底部时间线：彩点 + 事件） -->
                    <Border DockPanel.Dock="Bottom" BorderBrush="{StaticResource BorderLight}"
                            BorderThickness="0,1,0,0" Margin="0,8,0,0" Padding="0,6,0,0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Items.Count, ElementName=EventTimeline}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="抓包与测试事件时间线" Style="{StaticResource SectionTitleStyle}"/>
                            <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="EventTimeline">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <Ellipse Width="8" Height="8" Margin="0,0,8,0"
                                                         VerticalAlignment="Center"
                                                         Fill="{Binding DotBrush}"/>
                                                <TextBlock Text="{Binding Time}" FontSize="11"
                                                           Foreground="{StaticResource TextSecondary}"
                                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="{Binding Text}" FontSize="12"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                    <Grid Margin="0,6,0,0">
                    <DataGrid x:Name="ResultGrid" Style="{StaticResource DataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding No}" Width="34"/>
                            <DataGridTextColumn Header="检查项" Binding="{Binding Name}" Width="*"/>
                            <DataGridTemplateColumn Header="结论" Width="92">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding LevelBrush}" CornerRadius="10"
                                                Padding="9,2" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Level}" FontSize="11" Foreground="White"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="传输层" Binding="{Binding Transport}" Width="100"/>
                            <DataGridTextColumn Header="协议层" Binding="{Binding Protocol}" Width="100"/>
                            <DataGridTextColumn Header="耗时" Binding="{Binding Elapsed}" Width="76"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock Text="选择场景并填写目标后，点击“开始诊断”">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource EmptyStateStyle}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Items.Count, ElementName=ResultGrid}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    </Grid>
                </DockPanel>
            </Border>

            <!-- 证据面板 -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <DockPanel>
                    <Grid DockPanel.Dock="Top">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="诊断证据" VerticalAlignment="Center" Style="{StaticResource SectionTitleStyle}"/>
                        <Button Grid.Column="1" x:Name="AiButton" Content="AI 分析" IsEnabled="False"
                                Style="{StaticResource CompactButtonStyle}" Click="Ai_Click"
                                ToolTip="将诊断结果（已脱敏）发送给 AI 分析"/>
                    </Grid>
                    <Button DockPanel.Dock="Bottom" x:Name="ExportButton" Content="导出报告"
                            Style="{StaticResource PrimaryButtonStyle}" Margin="0,8,0,0"
                            IsEnabled="False" Click="Export_Click"/>
                    <TextBox x:Name="EvidenceBox" IsReadOnly="True" TextWrapping="Wrap"
                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                             BorderThickness="0" Background="Transparent" VerticalContentAlignment="Top"
                             FontFamily="Consolas" FontSize="12" Margin="0,6,0,0"
                             Tag="诊断完成后显示各检查项的观察事实与证据"/>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
